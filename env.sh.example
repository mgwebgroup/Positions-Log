SCRIPT_DIR=$(realpath $(dirname ${BASH_SOURCE[0]}))

JOURNAL=$(realpath "${SCRIPT_DIR}/../../Data/Positions/Journal_2026.csv")
META_FILE=$(realpath "${SCRIPT_DIR}/../../Data/Positions/meta.csv")
OHLCV_DIR=$(realpath "${SCRIPT_DIR}/../../Models/TM2/Data/OHLCV")

TIMESTAMP=
INSTRUMENT=
QUANTITY=
PRICE=
EXCHANGE_EXEC_ID=
# Configure default values here, or leave them blank:
TIMEZONE='-04:00'
COMMISSION='0.35'
CURRENCY=USD
ORDER_ID='abcdef01.02030405.abddef' # first digits usually are repeatable
ACCOUNT_NO=ABCD1
EXCHANGE_NAME=IBKRATS

