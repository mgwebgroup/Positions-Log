export ROOT_DIR=$(realpath "$(dirname ${BASH_SOURCE[0]})")
export LIB_DIR=$(realpath "${ROOT_DIR}/lib")

export JOURNAL=$(realpath "${ROOT_DIR}/../../Data/Positions/Journal_2026.csv")
export META_FILE=$(realpath "${ROOT_DIR}/../../Data/Positions/meta.csv")
export OHLCV_DIR=$(realpath "${ROOT_DIR}/../../Models/TM2/Data/OHLCV")
export AWKPATH="${LIB_DIR}/awk"

TIMESTAMP=
INSTRUMENT=
QUANTITY=
PRICE=
EXCHANGE_EXEC_ID=
# Configure default values here, or leave them blank:
TIMEZONE='-04:00'
COMMISSION='0.35'
CURRENCY=USD
ORDER_ID='abcdef01.02030405.abddef' # first digits usually are repeatable
ACCOUNT_NO=ABCD1
EXCHANGE_NAME=IBKRATS

